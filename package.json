{
  "name": "gavinhub",
  "private": true,
  "version": "1.0.0",
  "description": "轻量级个人浏览器新标签页",
  "scripts": {
    "self-check": "node scripts/self-check.mjs",
    "adapt-check": "node scripts/adapt-check.mjs",
    "test:regression": "node scripts/regression-test.mjs",
    "test:extension": "node scripts/extension-smoke.mjs",
    "check": "npm run self-check && npm run test:regression && npm run package:extension && npm run test:extension",
    "package:extension": "node scripts/package-extension.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
